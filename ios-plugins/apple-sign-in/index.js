// Stub entrypoint — the plugin is registered from the web app via
// `registerPlugin('AppleSignIn')` from @capacitor/core. This file exists
// so npm treats the package as well-formed. The Swift implementation in
// ios/Sources/AppleSignInPlugin is the actual plugin.
module.exports = {};
